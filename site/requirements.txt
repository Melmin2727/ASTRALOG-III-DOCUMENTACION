# =========================================================
# Dependencias para la documentación de AstraLog III
# Generado y validado el 2026-06-30
# Todas las versiones son ESTABLES (no Alpha/Beta/RC)
# =========================================================

mkdocs==1.6.1
mkdocs-material==9.7.6

# Plugins (mantenidos activamente y compatibles entre sí
# y con mkdocs-material 9.7.x / mkdocs 1.6.x)
mkdocs-minify-plugin==0.8.0
mkdocs-git-revision-date-localized-plugin==1.5.3
mkdocs-glightbox==0.5.2

# Pymdown Extensions: requerido por Material para
# admonitions, tabs, emoji, tareas, superfences (Mermaid), etc.
# Se deja sin fijar a un patch exacto para que pip resuelva
# automáticamente la versión compatible con mkdocs-material 9.7.6.
pymdown-extensions>=10.14,<12
